package com.example.grimpeurscyclingclubtest;

public class ParticipantAccount extends Account{
    public ParticipantAccount(String username) {
        super(username);
    }

    // We need to add more info for these accounts to properly interact with the event types, most notably, age, pace, level, etc.
}